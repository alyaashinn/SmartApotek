<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smart_apotek">

        <activity
            android:name=".admin.activity.DashboardAdminActivity"
            android:exported="false" />

        <activity
            android:name=".admin.activity.LaporanPenjualanActivity"
            android:exported="false" />

        <activity
            android:name=".admin.activity.DetailPesananActivity"
            android:exported="false" />

        <activity
            android:name=".admin.activity.KelolaPesananActivity"
            android:exported="false" />

        <activity
            android:name=".admin.activity.VerifikasiActivity"
            android:exported="false" />

        <activity
            android:name=".admin.activity.LoginAdminActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>
</manifest>